# ---- Core ----
python-dotenv==1.0.1
pydantic==2.8.2

# ---- Document processing ----
pypdf==4.3.1
langchain-text-splitters==0.2.2

# ---- Embeddings & Reranker (ONNX runtime, NOT torch - much lighter,
#      fits comfortably on free-tier hosts like Render's 512MB plan) ----
fastembed==0.8.0

# ---- Vector store (embedded/local mode, no server needed) ----
qdrant-client==1.19.0

# ---- Keyword search ----
rank-bm25==0.2.2

# ---- Reranking uses fastembed's cross-encoder module, already covered above ----

# ---- Knowledge graph ----
networkx==3.3
# neo4j==5.23.0   # optional, uncomment if you run a real Neo4j server

# ---- LLM clients (pick whichever you have a key for) ----
anthropic==0.69.0
openai==1.99.9
groq==0.31.1

# ---- API server ----
fastapi==0.112.1
uvicorn[standard]==0.30.6

# ---- Evaluation ----
rouge-score==0.1.2
numpy==1.26.4
